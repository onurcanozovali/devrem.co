# Firebase service boundary

Firebase initialization and feature adapters will live here in a later phase. UI and route modules must depend on feature-facing interfaces rather than importing Firebase SDKs directly.

No Firebase SDK or production credentials are configured in Phase 1.
