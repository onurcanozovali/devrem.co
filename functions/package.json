{
  "name": "devrem-functions",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "backfill:public-profiles": "pnpm run build && node lib/runPublicProfileBackfill.js",
    "backfill:devre-groups": "pnpm run build && node lib/runDevreGroupBackfill.js",
    "baseline:discovery-notifications": "pnpm run build && node lib/runNotificationBaseline.js",
    "test:push": "pnpm run build && node lib/runPushSmokeTest.js",
    "seed:discovery:clear": "pnpm run build && node lib/runDiscoverySeed.js clear",
    "build": "tsc",
    "lint": "eslint src --ext .ts",
    "seed:discovery": "pnpm run build && node lib/runDiscoverySeed.js seed",
    "seed:discovery:verify": "pnpm run build && node lib/runDiscoverySeed.js verify",
    "seed:chat": "pnpm run build && node lib/runGroupChatSeed.js seed",
    "seed:chat:clear": "pnpm run build && node lib/runGroupChatSeed.js clear"
  },
  "dependencies": {
    "@devrem/devre-domain": "file:devre-domain",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
